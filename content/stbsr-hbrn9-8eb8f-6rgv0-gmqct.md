# first steps on/with unusualstudio.net

installed git and created ec ssh key to check out noteball

installed lua5.3

modified quid script to compensate for lacking rng implementation pre-5.4

## pollen

`sudo apt-get install racket` which installs half a gig of graphical stuff, but whatever

`raco pkg install pollen` which pegs both cores and breaks my code-server session grr

https://docs.racket-lang.org/pollen/quick-tour.html

okay this thing expects me to type lozenges as a control character. lolno, aborting that

("it's not like this is APL" buddy, if it needs a symbol that's not on my keyboard it might as well be)

## rust

`curl --proto '=https' --tlsv1.2 https://sh.rustup.rs -sSf | sh`

install gcc so we have cc

git config global user.name and user.email so cargo will be cool

